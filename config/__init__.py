from config.defaults import (
    N_QUBITS,
    N_LAYERS,
    N_FEATURES,
    BATCH_SIZE,
    LEARNING_RATE,
    PATIENCE,
    DEFAULT_BACKEND,
    COLUMNS_TO_REMOVE,
)
from config.backends import get_backend, BACKENDS
