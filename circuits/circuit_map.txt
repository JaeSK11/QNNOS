╔══════════════════════════════════════════════════════════════════════════════════════╗
║            VARIATIONAL QUANTUM CIRCUIT — OS FINGERPRINTING CLASSIFIER              ║
║            20 qubits · 4 variational layers · 60 measurement outputs               ║
╚══════════════════════════════════════════════════════════════════════════════════════╝


══════════════════════════════════════════════════════════════════════════════════════
 COMPACT VIEW (4 qubits, 2 layers — same topology as full 20-qubit circuit)
══════════════════════════════════════════════════════════════════════════════════════

         ┌─────────┐  ┌─────────┐        ┌─────────┐  ┌─────────┐  ┌─────────┐        ┌─────────┐  ┌─────────┐  ┌─────────┐
q0: ─────┤ RY(θ₀) ├──┤         ├──●─────┤ RY(θ₀) ├──┤RY(w₀₀₀)├──┤RZ(w₀₀₁)├──●─────┤ RY(θ₀) ├──┤RY(w₁₀₀)├──┤RZ(w₁₀₁)├──●───── ⟨Z₀⟩ ⟨X₀⟩ ⟨Y₀⟩
         └─────────┘  │         │  │     └─────────┘  └─────────┘  └─────────┘  │     └─────────┘  └─────────┘  └─────────┘  │
                      │  RING   │  │                                             │                                             │
         ┌─────────┐  │  CNOT   │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐    │  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
q1: ─────┤ RY(θ₁) ├──┤  BLOCK  ├──⊕──┤ RY(θ₁) ├──┤RY(w₀₁₀)├──┤RZ(w₀₁₁)├──⊕─┼──┤ RY(θ₁) ├──┤RY(w₁₁₀)├──┤RZ(w₁₁₁)├──⊕─┼─── ⟨Z₁⟩ ⟨X₁⟩ ⟨Y₁⟩
         └─────────┘  │         │     └─────────┘  └─────────┘  └─────────┘    │  └─────────┘  └─────────┘  └─────────┘    │
                      │  0→1    │                                               │                                            │
         ┌─────────┐  │  1→2    │     ┌─────────┐  ┌─────────┐  ┌─────────┐    │  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
q2: ─────┤ RY(θ₂) ├──┤  2→3    ├─────┤ RY(θ₂) ├──┤RY(w₀₂₀)├──┤RZ(w₀₂₁)├────┼──┤ RY(θ₂) ├──┤RY(w₁₂₀)├──┤RZ(w₁₂₁)├────┼─── ⟨Z₂⟩ ⟨X₂⟩ ⟨Y₂⟩
         └─────────┘  │  3→0    │     └─────────┘  └─────────┘  └─────────┘    │  └─────────┘  └─────────┘  └─────────┘    │
                      │ (ring)  │                                               │                                            │
         ┌─────────┐  │         │     ┌─────────┐  ┌─────────┐  ┌─────────┐    │  ┌─────────┐  ┌─────────┐  ┌─────────┐    │
q3: ─────┤ RY(θ₃) ├──┤         ├─────┤ RY(θ₃) ├──┤RY(w₀₃₀)├──┤RZ(w₀₃₁)├────⊕──┤ RY(θ₃) ├──┤RY(w₁₃₀)├──┤RZ(w₁₃₁)├────⊕─── ⟨Z₃⟩ ⟨X₃⟩ ⟨Y₃⟩
         └─────────┘  └─────────┘     └─────────┘  └─────────┘  └─────────┘      └─────────┘  └─────────┘  └─────────┘

         ╰──ENCODE──╯  ╰──ENTANGLE──╯ ╰──REUPLOAD──╯╰────VARIATIONAL────╯╰──ENTANGLE──╯╰──REUPLOAD──╯╰────VARIATIONAL────╯╰──ENTANGLE──╯ ╰──MEASURE──╯
         ╰──────── INIT BLOCK ────────╯╰────────────────── LAYER 1 ──────────────────────╯╰────────────────── LAYER 2 ─────────────────────╯

         θᵢ = (xᵢ + 1)·π/2 for the ternary input xᵢ ∈ {-1, 0, 1}:  -1 (field absent) → |0⟩,  0 → equator,  1 → |1⟩
         (docs/DECISIONS.txt Decision 6; the earlier RY(xᵢ·π) encoding aliased -1 with +1 up to global phase.)


══════════════════════════════════════════════════════════════════════════════════════
 PENNYLANE qml.draw() OUTPUT (20 qubits, 4 layers) — first 5 wires shown
══════════════════════════════════════════════════════════════════════════════════════

 Init block           │ Layer 1                                              │ Layer 2 ...
                      │                                                      │
 0: ─╭AngleEmbed──╭●──╭X─╭AngleEmbed──RY──RZ─╭●──────────────────────────╭X─╭AngleEmbed──RY──RZ─╭●──... ─╭X─┤ <Z> <X> <Y>
 1: ─├AngleEmbed──╰X─╭●──│──├AngleEmbed──RY──RZ─╰X─╭●────────────────────│──├AngleEmbed──RY──RZ─╰X─...  │──┤ <Z> <X> <Y>
 2: ─├AngleEmbed─────╰X─╭●──│──├AngleEmbed──RY──RZ────╰X─╭●──────────────│──├AngleEmbed──RY──RZ────...  │──┤ <Z> <X> <Y>
    ...                  ...                               ...             ...                        ...
19: ─╰AngleEmbed────────────╰●─╰AngleEmbed──RY──RZ──────────────────╰X─╰●─╰AngleEmbed──RY──RZ────... ─╰●─┤ <Z> <X> <Y>

 (Ring wrap: q19 CNOT targets q0 at each entanglement stage)
 (Full qml.draw output is ~250 cols × 20 wires × 3 continuation blocks)


══════════════════════════════════════════════════════════════════════════════════════
 FULL 20-QUBIT STRUCTURAL DIAGRAM
══════════════════════════════════════════════════════════════════════════════════════

          INIT BLOCK                    LAYER l (repeated ×4, l = 0..3)
     ┌───────────────────┐    ┌────────────────────────────────────────────────┐
     │                   │    │                                                │
     │  ┌──────┐         │    │  ┌──────┐  ┌───────────┐  ┌───────────┐       │
q0 ──┤──┤RY(θ₀)├──●─────┤────┤──┤RY(θ₀)├──┤RY(w_l,0,0)├──┤RZ(w_l,0,1)├──●──┤───→ ⟨Z₀⟩⟨X₀⟩⟨Y₀⟩
     │  └──────┘  │     │    │  └──────┘  └───────────┘  └───────────┘  │  │
     │  ┌──────┐  ⊕  ●  │    │  ┌──────┐  ┌───────────┐  ┌───────────┐  ⊕  │
q1 ──┤──┤RY(θ₁)├─────┤──┤────┤──┤RY(θ₁)├──┤RY(w_l,1,0)├──┤RZ(w_l,1,1)├─────┤───→ ⟨Z₁⟩⟨X₁⟩⟨Y₁⟩
     │  └──────┘     ⊕  │    │  └──────┘  └───────────┘  └───────────┘     │
     │     ...       ... │    │     ...        ...             ...       ... │
     │  ┌──────┐         │    │  ┌──────┐  ┌────────────┐  ┌────────────┐   │
q19──┤──┤RY(θ₁₉)────────┤────┤──┤RY(θ₁₉)──┤RY(w_l,19,0)├──┤RZ(w_l,19,1)├──┤───→ ⟨Z₁₉⟩⟨X₁₉⟩⟨Y₁₉⟩
     │  └──────┘         │    │  └──────┘  └────────────┘  └────────────┘   │
     └───────────────────┘    └────────────────────────────────────────────────┘


══════════════════════════════════════════════════════════════════════════════════════
 RING CNOT ENTANGLEMENT TOPOLOGY (20 qubits)
══════════════════════════════════════════════════════════════════════════════════════

                            q0
                        ╱        ╲
                    q19              q1
                  ╱                      ╲
               q18                        q2
              ╱                              ╲
           q17                                q3
            │                                  │
           q16                                q4
            │                                  │
           q15                                q5
              ╲                              ╱
               q14                        q6
                  ╲                      ╱
                    q13              q7
                        ╲        ╱
                     q12 ── q11 ── q10 ── q9 ── q8

     CNOT direction: q_i → q_{(i+1) mod 20}
     (0→1, 1→2, 2→3, ... , 18→19, 19→0)

     After 4 layers of ring entanglement, each qubit is correlated
     with all others via paths of length ≤ 5 hops in each direction.


══════════════════════════════════════════════════════════════════════════════════════
 MEASUREMENT SCHEME
══════════════════════════════════════════════════════════════════════════════════════

     Each qubit is measured on 3 Pauli bases, yielding its full Bloch vector:

     Qubit i → ⟨Zᵢ⟩, ⟨Xᵢ⟩, ⟨Yᵢ⟩

     Output vector (60 elements):
     ┌──────────────────────┬──────────────────────┬──────────────────────┐
     │  ⟨Z₀⟩ ⟨Z₁⟩ ... ⟨Z₁₉⟩ │  ⟨X₀⟩ ⟨X₁⟩ ... ⟨X₁₉⟩ │  ⟨Y₀⟩ ⟨Y₁⟩ ... ⟨Y₁₉⟩ │
     │     indices 0–19     │     indices 20–39    │     indices 40–59    │
     └──────────────────────┴──────────────────────┴──────────────────────┘

     Each ⟨·⟩ ∈ [-1, +1]

     Bloch sphere interpretation per qubit:
            ⟨Z⟩ = cos(θ)              (population)
            ⟨X⟩ = sin(θ)cos(φ)        (real coherence)
            ⟨Y⟩ = sin(θ)sin(φ)        (imaginary coherence)


══════════════════════════════════════════════════════════════════════════════════════
 CONFIGURABLE VARIANTS (the diagrams above show the defaults)
══════════════════════════════════════════════════════════════════════════════════════

     --measure-axes Z        measure ⟨Z⟩ only → 20 outputs; adjoint-diff cost scales
                             with observable count, so this roughly halves epoch time
     --entangler crz         ring of trainable CRZ(w_l,i,2) instead of fixed CNOT
                             (3 params/qubit/layer; not weight-compatible with cnot)
     --entangler ring_long   fixed CNOT ring + long-range CNOT shortcuts i → (i+5) mod 20
                             and i → (i+10) mod 20 after every layer (--shortcut-offsets 5 10)
     --skip                  raw 20-feature vector concatenated with the expvals into the head
     --no-quantum --skip     head on raw features only (classical ablation)


══════════════════════════════════════════════════════════════════════════════════════
 GATE COUNTS (full 20-qubit, 4-layer circuit)
══════════════════════════════════════════════════════════════════════════════════════

     STAGE                      RY    RZ    CNOT
     ─────────────────────────  ────  ────  ────
     Initial encoding            20     0     0
     Initial ring entanglement    0     0    20
     Layer 1 (re-upload + var)   40    20    20
     Layer 2 (re-upload + var)   40    20    20
     Layer 3 (re-upload + var)   40    20    20
     Layer 4 (re-upload + var)   40    20    20
     ─────────────────────────  ────  ────  ────
     TOTAL                      180    80   100
     ─────────────────────────  ────  ────  ────

     Total gates:           360
     Single-qubit gates:    260  (180 RY + 80 RZ)
     Two-qubit gates:       100  (CNOT)
     Trainable parameters:  160  (4 layers × 20 qubits × 2 angles)
     Fixed parameters:      100  (20 initial + 80 re-upload RY from input)
     Measurements:           60  (20 Z + 20 X + 20 Y)
     Circuit depth per qubit: ~45 gates

     PennyLane-verified (qml.specs, v0.44.1):
       device_name:      default.qubit
       num_device_wires: 20
       num_gates:        265  (PL counts AngleEmbedding as 1 composite gate × 5 uses)
       depth:            113
       gate_types:       AngleEmbedding: 5, CNOT: 100, RY: 80, RZ: 80
       measurements:     expval(PauliZ): 20, expval(PauliX): 20, expval(PauliY): 20


══════════════════════════════════════════════════════════════════════════════════════
 DATA FLOW (end to end)
══════════════════════════════════════════════════════════════════════════════════════

     nPrint CSV (962 cols)
           │
           ▼
     Column removal (OsirisML indices)
           │
           ▼
     SelectKBest(k=20) → ternary feature vector x ∈ {-1,0,1}²⁰
           │
           ▼
     ┌─────────────────────────────────────────────────────────┐
     │  QUANTUM CIRCUIT (20 qubits)                           │
     │                                                         │
     │  Encode: RY(θᵢ), θᵢ = (xᵢ+1)·π/2, on qubit i          │
     │  Entangle: ring CNOT                                    │
     │  ×4 layers: re-upload → RY(w) → RZ(w) → ring CNOT     │
     │  Measure: ⟨Z⟩,⟨X⟩,⟨Y⟩ on all qubits                  │
     │                                                         │
     │  Output: 60-dim vector ∈ [-1,1]⁶⁰                     │
     └───────────────────────┬─────────────────────────────────┘
                             │
                             ▼
     ┌─────────────────────────────────────────────────────────┐
     │  CLASSICAL HEAD                                         │
     │                                                         │
     │  Linear(60, 128) → ReLU → Dropout(0.3) → Linear(128,C) │
     │                                                         │
     │  Output: logits ∈ ℝ^C  (C = number of OS classes)      │
     └─────────────────────────────────────────────────────────┘
